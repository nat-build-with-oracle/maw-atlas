# Birth note

Producer/ingest-keeper: maw-atlas route backfill hardening (429 backoff), mirror-ingest-sweep cron ownership, gap-detection, ~125k pre-toRow-fix row re-backfill. Consumer side stays with mirror-oracle.
